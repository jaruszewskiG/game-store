#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Ensure staged files are formatted (lint-staged runs prettier --write and git add)
npx --no-install lint-staged
#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run tests
npm test

# Run lint-staged (formatting)
npx --no-install lint-staged
